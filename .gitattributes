# Force LF for shell scripts so they run on macOS/Linux
*.sh        text eol=lf
*.bash      text eol=lf

# Force CRLF for Windows batch files
*.bat       text eol=crlf
*.cmd       text eol=crlf

# Default: let git auto-detect, but use LF in repo
*           text=auto
